{
  "//": "Typecheck-only config covering tests; the build config includes just src.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "noEmit": true
  },
  "include": ["src", "tests"]
}
